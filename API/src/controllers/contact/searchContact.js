// const getAllContact = require('./getAllContact');

// const searchContact = async (search) => {
//     const allContacts = await getAllContact();
//     if (search){
//         const contactFiltered = allContacts.filter((contact) => {
//             const lowerCaseSearch = search.toLowerCase();
//             return (
//                 contact.name.toLowerCase().includes(lowerCaseSearch) ||
//                 contact.email.toLowerCase().includes(lowerCaseSearch) ||
//                 contact.phone.toLowerCase().includes(lowerCaseSearch)
//             )
//         })

//     return contactFiltered;
// }
// };

// module.exports = searchContact