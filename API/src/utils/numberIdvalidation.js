// const numberIdValidation = (id) => {
//     if(!id) throw new Error ('Missing id');
//     if(isNaN(id)) throw new Error ('Id must be a number');
//     return id
// }

// module.exports = numberIdValidation;